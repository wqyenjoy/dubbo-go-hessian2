主题: [DISCUSS] Hessian2编解码器性能优化 - 对象池化与预分配

收件人: dev@dubbo.apache.org
抄送: @wongoo @AlexStocks

尊敬的Dubbo社区维护者：

我已完成了对dubbo-go-hessian2的一项重要性能优化工作，通过对象池化、内存预分配和生命周期管理等技术手段，显著提升了Hessian2编解码器的性能。现将相关情况汇报如下，并希望得到您的反馈和建议。

## 优化概述

本次优化主要包括：

1. **对象池化**：实现Decoder/Encoder对象池，减少对象创建和GC压力
2. **缓冲区池化**：实现大小分层的缓冲区池，减少大型字节数组的分配
3. **预分配优化**：为列表和映射添加合理的预分配策略，避免过度分配
4. **引用管理**：优化refHolders生命周期，避免内存泄漏
5. **环境变量控制**：通过`HESSIAN_POOL=1`环境变量开启池化功能，默认不启用

## 性能提升

通过全面的基准测试，我们观察到以下性能提升：

- **大型对象解码**: 性能提升 **60.0%**，内存减少 **57.4%**
- **并行请求处理**: 性能提升 **31.5%**，内存减少 **51.3%**
- **标准请求处理**: 性能提升 **11.0%**，内存减少 **51.2%**

## 兼容性与风险控制

为确保兼容性和稳定性，我们采取了以下措施：

1. **默认不启用**: 通过环境变量显式开启，默认保持现有行为不变
2. **API兼容性**: 保持现有API不变，只在内部实现优化
3. **线程安全**: 通过竞态检测测试验证无数据竞争
4. **资源释放**: 在PutDecoder和PutEncoder中彻底清理资源
5. **容量限制**: 为集合添加合理的预分配上限，避免过度分配

## 下一步计划

我计划创建一个PR，标题为"perf: add Decoder/Encoder pool & prealloc"，并附上详细的性能测试报告。在此之前，我想先听取社区维护者的意见和建议，特别是关于以下几点：

1. 这种优化方向是否符合项目的发展规划？
2. 是否有其他需要考虑的兼容性或风险因素？
3. 是否有其他需要补充的测试场景？

完整的性能测试报告和代码实现已经准备就绪，如有需要可随时提供更多详细信息。

期待您的回复和建议！

此致

敬礼 